/** 硬件优化（LLM 系统工程）评测 — 题目与评委/汇总模板（与 evaluationPresets 组合使用） */

export const EVAL_PRESET_LLM_SYS_GPU_SPARSE = "llm-sys-gpu-sparse-attention";
export const EVAL_PRESET_LLM_SYS_KV_CLUSTER = "llm-sys-distributed-kv-cache";

export const TASK_LLM_SYS_GPU_SPARSE = `题目名称：面向超长上下文LLM推理的稀疏注意力GPU算子优化

背景
随着大模型上下文长度扩展至百万级甚至千万级Token，标准自注意力的平方复杂度成为推理瓶颈。稀疏注意力机制（如窗口注意力、哈希注意力、分层注意力）虽可降低理论复杂度，但在GPU上的实际实现面临严重挑战：

不规则的内存访问模式导致缓存利用率低

稀疏矩阵格式（如CSR、COO）在GPU上需大量原子操作或动态内存分配

变长序列的批量推理难以高效并行

现有开源实现（如FlashAttention‑2、xFormers）主要针对稠密注意力，稀疏版本性能往往远低于理论峰值。

任务
设计并实现一个高性能GPU算子（CUDA/Triton），用于加速超长上下文（≥128K Token）下的稀疏注意力计算。算子需支持以下特性之一（可自选）：

动态稀疏模式：注意力矩阵的非零块位置由运行时实时计算（如基于哈希或可学习掩码）

变长序列批处理：同时处理多个长度不同的序列，且每个序列的稀疏模式可独立

混合精度与内存优化：支持FP16/BF16，并最小化中间显存占用

输出要求
算子设计思路：稀疏表示格式、线程块划分策略、内存访问优化方法

伪代码或关键代码片段（展示核心循环或内存访问模式）

理论性能分析：FLOPs利用率、显存带宽占用、预期加速比（相比稠密FlashAttention）

可选：给出在NVIDIA A100/H100上的预期性能数据（如Token/s）

评分标准（1‑10分）
分数	门槛描述
1‑3分	完全错误：不了解GPU编程基本概念；提出的方案无法在GPU上运行；忽略稀疏性
4分	有明显缺陷：仅描述理论算法无具体算子设计；未考虑线程束分歧、共享内存冲突等关键GPU特性
5分	勉强触及问题：给出了基本的稀疏格式（如CSR）和简单Kernel；但未针对稀疏注意力做专门优化，性能预计远差于FlashAttention
6分（及格）	基础合格：算子设计完整，包含稀疏表示、线程块映射、合并内存访问；能正确计算注意力；但未利用现代GPU特性（如Tensor Core、异步拷贝），或未解决负载不均衡问题
7分（良好）	有亮点：在6分基础上，至少满足一项：① 针对稀疏模式设计了专用的负载均衡策略（如基于非零块数量的动态调度）；② 使用Tensor Core进行稀疏矩阵乘法加速；③ 采用异步拷贝与流水线隐藏访存延迟
8分（优秀）	发表级：算子设计具有明显创新（如自定义稀疏存储格式、融合稀疏注意力与KV缓存管理）；性能分析详细，与基线（FlashAttention‑2稀疏模式）对比有可量化的优势（≥1.5倍）；代码结构清晰，具备工业级可维护性
9分（卓越）	风格与洞见：在8分基础上，算子设计揭示了稀疏注意力在GPU上达到近理论峰值的关键突破；提出新原语或抽象（如专用于稀疏注意力的PTX指令级优化），可被广泛复用于其他稀疏算子
10分	完美传世：算子性能在主流GPU上达到或超越手工汇编极限，且设计优雅通用；开源后成为业界标准（如被主流推理框架采纳）；此分数极少授予`;

export const TASK_LLM_SYS_KV_CLUSTER = `题目名称：千卡级集群下超长上下文KV缓存的分布式存储与读写优化方案

背景
在部署大规模语言模型服务时（如数千张GPU并发处理百万级上下文的对话），KV缓存（Key‑Value Cache）成为关键瓶颈：

容量爆炸：单条长上下文KV缓存可达数十GB，多请求并发时远超单卡显存

通信开销：传统做法将KV缓存在GPU间通过NVLink/IB传输，带宽昂贵且延迟敏感

弹性扩缩：不同请求上下文长度差异大，静态分配导致显存碎片和浪费

现有方案（如vLLM的PagedAttention、SGLang的RadixAttention）优化了单机场景，但在超大规模集群（≥1000 GPU）中，跨节点KV缓存的高效读写仍未解决。

任务
设计一套分布式KV缓存系统，目标是在千卡级GPU集群上支持：

同时处理≥1000个请求，每个请求上下文长度在1K～1M Token之间动态变化

任意请求可在集群任意GPU上调度，KV缓存需支持跨节点、低延迟访问

支持前缀共享（Prefix Caching）以复用公共上下文（如系统提示词）

系统约束
硬件：每节点8×H100（80GB显存），节点间通过InfiniBand（200Gbps）互连

软件：PyTorch分布式环境，支持CUDA IPC与NCCL

输出要求
总体架构设计：KV缓存的分布式元数据管理、数据分片策略、跨节点访问协议

读写路径优化：如何最小化跨节点通信次数？是否使用RDMA、GPU Direct RDMA等技术？

缓存淘汰与预取策略：结合请求到达模式的设计

性能预估：最大并发请求数、平均首次Token生成时延、吞吐量（Token/s）

评分标准（1‑10分）
分数	门槛描述
1‑3分	完全错误：不了解KV缓存基本概念；方案无法在集群规模下扩展
4分	有明显缺陷：仅将单机PagedAttention扩展为多机简单复制；未考虑跨节点通信瓶颈；无元数据管理
5分	勉强触及问题：提出了基于一致性哈希的分片策略，但读写路径均需跨节点RPC，延迟不可控
6分（及格）	基础合格：完整系统设计，包含元数据服务、数据分片、基本通信机制；能处理前缀共享；但未采用RDMA或GPU Direct，预计读写延迟高（≥10ms）
7分（良好）	有亮点：在6分基础上，至少满足一项：① 使用RDMA或GPUDirect RDMA实现零拷贝跨节点传输；② 设计了基于注意力模式的自适应缓存预取；③ 通过异步流水线隐藏通信延迟
8分（优秀）	发表级：方案具有明显创新（如解耦元数据与数据平面、利用可编程交换机加速缓存路由）；性能预估量化细致，与基线（如纯NCCL广播）对比有明显提升（延迟降低50%以上）；设计考虑容错与弹性扩缩
9分（卓越）	风格与洞见：在8分基础上，方案揭示了大规模KV缓存与数据密集型系统设计的新范式（如将KV缓存作为“持久化内存”管理）；提出可证明的性能上界或调度最优性条件
10分	完美传世：系统设计在千卡集群上验证（或仿真）达到业界顶尖水平（如支持10K并发、首Token时延<500ms）；设计文档成为大规模LLM服务系统的参考架构；此分数极少授予`;

export const LLM_SYSTEMS_JUDGE_SYSTEM = `你是一位长期负责大规模推理与系统基础设施的「首席技术审稿人」，熟悉 CUDA/Triton、GPU 体系结构、分布式存储与 RDMA。你极其吝啬分数，厌恶缺少量化、缺少瓶颈分析、或无法落地的架构空谈。6 分在你眼中仅表示「勉强及格、能自洽但工程价值有限」。你的任务是评审「硬件优化」方向的书面答卷（可能对应稀疏注意力 GPU 算子，或千卡级分布式 KV 缓存），须用同一套标准衡量。

评分维度（权重分配）
在给出具体分值前，必须从以下四个维度进行拆解分析（权重不可忽略）：

问题理解与创新性（30%）：是否准确把握稀疏注意力在 GPU 上的真实瓶颈，或大规模 KV 在集群上的容量/通信/调度矛盾；方案是否有实质工程创新，而非仅复述 FlashAttention/vLLM 名称。

技术严谨性（25%）：算子设计是否涉及正确的线程模型、访存合并、占用与同步；分布式设计是否交代一致性、故障与元数据路径；是否避免夸大或不可验证的断言。

工程可行性与性能（25%）：是否给出可实现的模块划分、关键路径延迟与带宽量级估算；是否量化对比基线（如稠密 FA、单机 PagedAttention、NCCL 广播）；是否说明硬件约束（FP16/BF16、IB 带宽、显存上限）。

表述与完整性（20%）：结构是否清晰；是否覆盖题目要求的输出项（设计思路、代码/伪代码、性能分析或架构与预估等）。

1–10 分 详细评分标准（门槛说明；适用于 GPU 算子题与 KV 集群题）

1–3 分（完全不合格）
基本概念错误（如忽略 GPU 可执行性、忽略 KV 在多机下的扩展性）；或方案与题目无关；几乎无量化分析。

4 分（有明显缺陷）
仅有高层算法描述而无算子/系统细节；未讨论 warp 分歧、共享内存、跨节点通信等关键瓶颈；或仅把单机方案平铺到多机而无元数据与路径设计。

5 分（勉强触及）
能列出 CSR/简单 kernel 或一致性哈希等，但未针对稀疏注意力或 KV 长尾延迟做专门优化；性能预估明显弱于业界基线且无改进路径。

6 分（及格）
提交完整设计：算子侧含稀疏表示、块映射与合并访存；或系统侧含元数据、分片与前缀共享机制。但缺少 Tensor Core/RDMA/异步流水等现代手段，或负载均衡、跨节点延迟问题讨论不足。

7 分（良好）
在 6 分基础上至少一项突出：动态负载均衡、Tensor Core/异步拷贝、RDMA/GPUDirect、自适应预取、流水线隐藏通信等；与基线或同类工作有定量对比意识。

8 分（优秀）
具备「可投系统顶会」水准：算子侧有自定义格式或融合优化、与 FlashAttention-2 稀疏基线有 ≥1.5× 等量化优势；或系统侧有元数据/数据平面解耦、可量化延迟收益（如较纯 NCCL 显著降低）。考虑容错与可维护性。

9 分（卓越）
在 8 分基础上展现范式级洞察：如新原语/指令级抽象、或将 KV 作为分布式持久化对象管理；提出可检验的性能上界或调度条件；结果可被其他稀疏/系统任务复用。

10 分（理论上限，极少授予）
声称达到或逼近硬件极限、并被工业界广泛采纳的潜力；在 LLM Arena 中除非全文几乎无懈可击，否则默认最高不宜超过 9.5 分；10 分保留为天花板。`;

export const LLM_SYSTEMS_JUDGE_USER = `【硬件优化答卷】
{{candidate}}

请根据上述评审标准，先按四个维度逐项分析，再给出 1–10 分的总分与简要结论。`;

export const LLM_SYSTEMS_AGGREGATOR_SYSTEM = `你是一个结果汇总助手。你的任务是将多位 Judge 对同一篇硬件优化方向答卷的评分与评语进行简洁归纳。
要求：
1. 只整理评委的原意和分数，不加入自己的任何评价、判断或补充。
2. 按评委模型名称分别列出，不得使用「评委1」「评委2」等匿名指代。
3. 总字数不超过200字。
4. 输出语言为中文。`;

export const LLM_SYSTEMS_AGGREGATOR_USER = `【硬件优化答卷】
{{candidate}}

【各评委评价】
{{reviews}}

请按上述要求，汇总每位评委的意见与分数，只呈现评委模型原名及对应内容，不做额外评价。`;

export function getLlmSystemsAggregatorPartial(): {
  systemPrompt: string;
  userPromptTemplate: string;
} {
  return {
    systemPrompt: LLM_SYSTEMS_AGGREGATOR_SYSTEM,
    userPromptTemplate: LLM_SYSTEMS_AGGREGATOR_USER,
  };
}
